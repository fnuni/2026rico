# Python Dependencies for Hybrid Neural-Heuristic TSP Solver
# ============================================================
# Paper: "Hybrid Neural–Heuristic TSP Solver with Attention-Guided 
#         Refinement for Real-Time AMR Coordination"
# Authors: BLIND REVIEW
# Institution: BLIND REVIEW
# Year: 2026

# Core Deep Learning Framework
# -----------------------------
torch>=2.0.0                 # Transformer encoder-decoder (Section 4.2)
torchvision>=0.15.0          # Optional: data augmentation

# Numerical Computing
# -------------------
numpy>=1.21.0                # Array operations, distance computations
scipy>=1.7.0                 # Optional: optimization utilities

# Data Processing
# ---------------
pandas>=1.3.0                # Dataset loading and manipulation
h5py>=3.7.0                  # Optional: HDF5 data storage

# High-Performance Computing
# --------------------------
numba>=0.56.0                # JIT compilation for 2-opt (Section 5.1)

# Visualization (for analysis)
# ----------------------------
matplotlib>=3.5.0            # Plotting (Figure 2, Figure 4)
seaborn>=0.11.0              # Statistical plots

# Utilities
# ---------
tqdm>=4.64.0                 # Progress bars during training
pyyaml>=6.0                  # Configuration files

# Hardware Optimization (platform-specific)
# -----------------------------------------
# For Apple Silicon (M1/M2):
# - torch with MPS backend (included in torch>=2.0.0)

# For NVIDIA GPUs:
# - torch with CUDA support (install CUDA-specific version)
# Example: pip install torch --index-url https://download.pytorch.org/whl/cu118

# For NVIDIA Jetson (embedded deployment):
# tensorrt>=8.5.0            # INT8 quantization (Section 5.1)

# Development Tools (optional)
# ----------------------------
pytest>=7.0.0                # Unit testing
black>=22.0.0                # Code formatting
mypy>=0.990                  # Type checking

# Notes
# -----
# 1. Install PyTorch first with appropriate backend (CPU/CUDA/MPS)
# 2. TensorRT only needed for embedded deployment on Jetson
# 3. For training: GPU with ≥8GB VRAM recommended
# 4. For inference: CPU sufficient for N ≤ 100

# Installation
# ------------
# Standard (CPU):
#   pip install -r requirements.txt
#
# With CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# Apple Silicon (MPS):
#   pip install -r requirements.txt  # MPS included in torch>=2.0.0
#
# Jetson (ARM):
#   # Install PyTorch for Jetson from NVIDIA
#   pip install -r requirements.txt --extra-index-url https://...
